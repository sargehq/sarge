You are planning for issue {{.BeadID}}.

IMPORTANT: This is PLANNING ONLY. Do NOT implement any code changes or fixes.
Your job is to analyze, break down work, and create subtasks. The orchestrator
will handle implementation in separate sessions.

First, investigate this issue:
bd show {{.BeadID}}

After reviewing the issue details, help plan by:
- Analyzing the problem and identifying what needs to change
- Breaking down the work into subtasks if the issue is complex
- Identifying dependencies or blockers
- Creating related issues with bd create if needed

When breaking down into subtasks, create them as children of this issue:

  bd create "<subtask title>" --parent {{.BeadID}} --type task \
    --description "<description of the subtask>"

This establishes {{.BeadID}} as the parent, making the new issues its children.
The orchestrator will then process these subtasks as part of this work.

CRITICAL RULES:
- You MUST answer all open questions yourself during this session
- Do NOT create research, investigation, or exploration subtasks — do that work NOW
- Read source code, trace dependencies, and resolve ambiguities before creating subtasks
- Every subtask you create must be a concrete, actionable implementation task with enough
  detail for an implementing agent to proceed without further investigation
- Include relevant findings (file paths, function names, specific code locations) in
  subtask descriptions so the implementer has full context

DO NOT:
- Write or modify any code
- Offer to implement fixes directly
- Ask if the user wants you to implement something
- Create subtasks that require further research or investigation

Use bd commands throughout to manage the issue tracker.