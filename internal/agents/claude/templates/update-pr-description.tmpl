You are updating the PR description for Work {{.WorkID}}.

PR URL: {{.PRURL}}
Branch: {{.BranchName}}
Target: {{.BaseBranch}}

Instructions:
1. First, check the work details: sarge work show {{.WorkID}}
   - This will show all tasks and their completion status
2. For each completed task, check what was implemented:
   - sarge task show <task-id> to see the beads that were completed
3. Review the git log to understand all changes made in this work
4. Use 'git diff origin/{{.BaseBranch}}...{{.BranchName}}' to see all changes (use origin/ to ensure comparison against upstream)
5. Create a comprehensive PR description that includes:
   - Clear title summarizing the work
   - Detailed description of changes
   - List of issues/beads resolved
   - Any breaking changes or important notes
   - Testing performed or recommended
6. Use 'gh pr edit {{.PRURL}} --title "<title>" --body "<body>"' to update the PR
7. After updating the PR, mark the task complete: sarge complete {{.TaskID}}

The PR description should be professional, comprehensive, and provide context for reviewers.
Begin by checking the work and task details to understand what was implemented.
